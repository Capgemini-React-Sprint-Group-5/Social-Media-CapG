# Copy this to .env and set the backend base URL
# The vite proxy (vite.config.js) handles /api/* during dev,
# so you usually only need to change this for production builds.
VITE_API_BASE_URL=http://localhost:3000
